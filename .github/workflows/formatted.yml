name: Check Formatted Code
on: [push]
jobs:
  Formatted:
    runs-on: ubuntu-latest
    steps:
      - name: "Install modules"
        run: "yarn install"
      - name: "Prettier"
        run: "yarn check:style"
